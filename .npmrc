# Keep everything self-contained inside this folder.
# Local package cache so deleting /home/anon/aegis wipes ALL downloads too —
# nothing leaks into ~/.npm.
cache=./.npm-cache
# never install anything globally from here
global=false
